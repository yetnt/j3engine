package com.j3d.engine.events;

/**
 *
 */
public enum ObjectType {
    /**
     * Registers the listener as a NODE
     */
    NODE,
    /**
     * Registers the listener as a PARENT
     */
    PARENT
}
